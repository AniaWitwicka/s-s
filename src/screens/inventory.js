import { store } from '../store.js'

export function render(container) {
  container.innerHTML = `<div class="screen"><p style="padding:1rem;font-family:var(--s-font-ui);color:var(--s-color-text-muted)">inventory — coming soon</p></div>`
}
